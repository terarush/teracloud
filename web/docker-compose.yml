services:
  web:
    image: teracloud-app:latest
    build:
      context: .
      target: runtime
    container_name: teracloud-web
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"